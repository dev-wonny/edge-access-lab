{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Worker (wrangler dev)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}/worker"
    }
  ]
}
